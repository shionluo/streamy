// Import
import { combineReducers } from 'redux';

// Reducers

// ----------------------------------------------------------------------------------------- //

const rootReducer = combineReducers({
  replaceMe: () => 'Hiiii',
});

export default rootReducer;
